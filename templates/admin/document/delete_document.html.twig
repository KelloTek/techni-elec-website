{% extends 'profile-base.html.twig' %}

{% block title %}
    Admin - {{ 'admin.document.delete.title'|trans() }}
{% endblock %}

{% block name %}
    {{ 'admin.document.delete.name'|trans() }}
{% endblock %}

{% block body %}
    {% embed 'components/dashboard/form.html.twig' with {
        title: 'admin.form.delete_document.title'|trans({}, 'forms'),
        description: 'admin.form.delete_document.description'|trans({}, 'forms'),
        submit: 'admin.form.delete_document.submit'|trans({}, 'forms'),
        form: form
    } %}
        {% block form %}
            {{ form_row(form.name) }}
        {% endblock %}

        {% block afterForm %}
            <p class="text-center">
                <a href="{{ path('app_admin_documents') }}" class="hover:text-custom-orange-100">
                    {{ 'admin.form.delete_document.back'|trans({}, 'forms') }}
                </a>
            </p>
        {% endblock %}
    {% endembed %}
{% endblock %}
